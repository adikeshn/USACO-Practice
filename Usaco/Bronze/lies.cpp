#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main(){
    int N, numG = 0, numL = 0;
    cin >> N;
    vector<pair<int, char>> stuff(N);
    for (int x = 0; x<N; x++){
        cin >> stuff[x].first >> stuff[x].second; 
    }
    

}